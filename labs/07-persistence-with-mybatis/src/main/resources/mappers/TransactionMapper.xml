<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fidelity.leap.sprint6.TransactionMapper">

    <!-- KATA B - XML-based mapper.
         Write the <select> for findByAccountId(int accountId).
         Join transactions -> instruments (on instrument_id) so you can
         return the instrument's ticker alongside each transaction.
         Select: transaction_id, i.ticker AS ticker, txn_type, quantity,
         price, txn_date - filtered WHERE t.account_id = #{accountId},
         ordered by txn_date.

         TODO: replace this stub with a real <select id="findByAccountId">. -->

</mapper>

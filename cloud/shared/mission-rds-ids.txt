# Module 9 (persistent, free): DB subnet group and RDS security group -
# kept in place for reuse; no hourly charge while idle.
DB_SUBNET_GROUP=leap-mission-db-subnets
SG_RDS=sg-0e9ce2dc33a3f152d
# leap-rds-sg: allows 5432 from leap-app-sg (sg-0f1f079da1598dec2) only

# Module 9 (billed - torn down after verification; recreated by Module
# 11's deployment automation script when needed):
# RDS_INSTANCE=leap-mission-db (deleted, --skip-final-snapshot)
# MASTER_USER_SECRET (deleted automatically with the RDS instance)
# EXECUTION_ROLE_POLICY=leap-read-db-secret (removed - referenced the
#   now-deleted secret's ARN)

# Task definitions registered this module (left registered, no cost):
# leap-mission-service:3 - adds "secrets" (DB_PASSWORD) and "environment"
#   (DB_HOST/DB_PORT/DB_NAME/DB_USERNAME) to the container definition
# leap-db-connectivity-check:1-3 - one-off family used to diagnose the
#   entryPoint-override and /dev/tcp findings; revision 3 is the working
#   nc-based check

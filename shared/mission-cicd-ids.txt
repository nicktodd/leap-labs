# Module 11 (persistent, free): IAM role + instance profile for Jenkins -
# no static AWS credentials involved, no hourly charge.
JENKINS_ROLE=leap-jenkins-deploy-role
JENKINS_ROLE_ARN=arn:aws:iam::<account-id>:role/leap-jenkins-deploy-role
JENKINS_INSTANCE_PROFILE=leap-jenkins-deploy-profile
# Trust policy: ec2.amazonaws.com only. Attach this instance profile to
# the EC2 instance running the Jenkins agent - no AWS keys stored in
# Jenkins credentials at all.
# Permissions policy: leap-jenkins-deploy-policy (ECRAuth, ECRPush,
# ECSDeploy, PassExecutionRoleOnly)

# Task definition history (leap-mission-service family):
# revision 4: real failure - cloned rev 3's stale Secrets Manager reference
#   (secret was deleted with Module 9's RDS teardown) -> AccessDeniedException
# revision 5: clean, built from the current live definition, no stale
#   secrets - verified RUNNING with real startup logs

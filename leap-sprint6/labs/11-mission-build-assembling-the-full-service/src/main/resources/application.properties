spring.application.name=mission-service

# Your local Postgres install from Sprint 3 — adjust username/password to match your own setup
spring.datasource.url=jdbc:postgresql://localhost:5432/mission
spring.datasource.username=postgres
spring.datasource.password=postgres

mybatis.mapper-locations=classpath:mappers/*.xml
mybatis.configuration.map-underscore-to-camel-case=true

jwt.shared-secret=mission-control-shared-secret-key-32-bytes-minimum

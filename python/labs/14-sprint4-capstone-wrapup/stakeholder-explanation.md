# Stakeholder Explanation - Python Capstone Dashboard

> Team task. Write this in PLAIN LANGUAGE - no unexplained acronyms or jargon. Every claim
> should be specific to this dataset and this team's choices, not a generic statement.
> Delete these instructions once you've written your answers.

## Why we read the data the way we did

<!-- TODO: Explain why you accessed the data from a local file rather than through the
     REST API you built in Module 11 (or vice versa, if you chose the API). Give a
     SPECIFIC reason grounded in this dataset - its size, how often it changes, what it
     would cost to run a server - not a generic "files are simpler". -->

## Why we are confident the data is trustworthy

<!-- TODO: Explain what makes you confident in the numbers on your dashboard. Reference
     the actual cleaning steps from Module 6 and/or the validation tests from Module 12
     that your pipeline runs. Name what each step catches, in plain language - e.g. what
     a duplicate row would have done to your totals if it had gone unnoticed. -->
